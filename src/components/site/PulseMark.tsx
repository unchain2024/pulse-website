/** Official Pulse symbol, derived from the supplied SVG. */
export function PulseMark({className=''}:{className?:string}){return <img className={`pulse-brand-mark ${className}`} src="/assets/pulse-mark.svg" width={28} height={32} alt="" aria-hidden="true"/>;}
